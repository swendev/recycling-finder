// setup config
var config          = {};

// adding arrays for configurable vars
config.db           = {};

// setting up db params
config.db.url       = "127.0.0.1";
config.db.port      = "27017";
config.db.name      = "location";
config.db.username  = "";
config.db.password  = "";

module.exports = config;